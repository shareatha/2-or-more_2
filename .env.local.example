# Copy this file to .env.local and fill in real values.
# Web App URL from the Google Apps Script deployment in
# google-apps-script/Code.gs — it powers the consultation inquiry form
# (logs to a Sheet, emails the business, emails the submitter).
NEXT_PUBLIC_APPS_SCRIPT_URL=
